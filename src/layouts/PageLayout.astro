---
import BaseLayout from './BaseLayout.astro';
import CompareWidget from '../components/CompareWidget.astro';

interface Props {
  title: string;
  metaTitle?: string;
  description?: string;
  intro?: string;
  compare?: boolean;
  wide?: boolean;
}
const { title, metaTitle, description = '', intro = '', compare = false, wide = false } = Astro.props;
---
<BaseLayout title={metaTitle || title} description={description}>
  <section class="page-hero">
    <div class="wrap">
      <p class="crumbs"><a href="/">Home</a><span>/</span><span style="opacity:1;color:#a9c1cf">{title}</span></p>
      <h1>{title}</h1>
      {(intro || description) && <p>{intro || description}</p>}
    </div>
  </section>

  <div class="article">
    <div class={wide ? 'wrap' : 'wrap article-layout'}>
      <article>
        <div class="prose" style={wide ? 'max-width:none' : undefined}>
          <slot />
        </div>
        {compare && <CompareWidget />}
      </article>
      {!wide && (
        <aside class="aside">
          <div class="box accent">
            <h3>Direct vergelijken</h3>
            <p>Zie in 3 minuten welke uitvaartverzekering het beste bij je situatie past.</p>
            <a class="btn" href="/#vergelijken">Start vergelijking</a>
          </div>
          <div class="box">
            <h3>Populair</h3>
            <div class="links">
              <a href="/begrafenisverzekering/">Begrafenisverzekering</a>
              <a href="/overlijdensrisicoverzekering/">Overlijdensrisicoverzekering</a>
              <a href="/begrafenisondernemer/">Begrafenisondernemer zoeken</a>
              <a href="/kennisbank/">Naar de kennisbank</a>
            </div>
          </div>
        </aside>
      )}
    </div>
  </div>
</BaseLayout>
